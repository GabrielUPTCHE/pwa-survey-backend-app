spring.application.name=springboot-survey-app

# MongoDB — solo para GridFS (binarios de evidencia)
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://juan23as2_db_user:5RItUR59xA64Ys4P@cluster0.8mknl71.mongodb.net/?appName=Cluster0}
spring.data.mongodb.database=${MONGODB_DATABASE:pwa}

# PostgreSQL / Supabase — MISMA base de datos que el dashboard (fuente de verdad)
spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-us-east-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.xbkhuxjuizrkowrfbaui}
spring.datasource.password=${DB_PASSWORD:survey12@sdr34443w3}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.datasource.hikari.maximum-pool-size=3

# Server
server.port=${PORT:9090}

# JWT
jwt.secret.key=${JWT_SECRET:nZh602fMAahjh9EHPfJsmm7Zxa0cKxRsC65NU/ZSdpjk/Cxjogce3qEXXAo4TKVF}
jwt.time.expiration=${JWT_EXPIRATION:86400000}

# Multipart (para subida de evidencias)
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=100MB

# CORS — lista separada por comas de orígenes permitidos.
# En dev con proxy Vite no hace falta (same-origin). En prod: http://mi-front.com
app.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:}

# Cookie — en dev: Lax + no-secure. En prod cross-origin: None + secure
app.cookie.same-site=${COOKIE_SAME_SITE:Lax}
app.cookie.secure=${COOKIE_SECURE:false}

# Logging
logging.level.org.springframework.data.mongodb=WARN
logging.level.com.survey=DEBUG
